---
description: "Create an implementation plan with execution profile and approval."
argument-hint: "<task, spec path, or plan request>"
disable-model-invocation: true
---

Read the file at `${CLAUDE_PLUGIN_ROOT}/skills/ralplan/SKILL.md` using the Read tool and follow its instructions exactly.

## User Input

$ARGUMENTS
